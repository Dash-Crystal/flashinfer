# Phase 0 + round 1 synthesis — `claude/mixed-kv-sm90-tma` @ 82b8d660 (2026-09-03)

Sources: measurement JSON (P0.1/3/4/5/6/7/8), track reports (A, B, D, E), commit bodies b54d0717 / 6c3cc82a / f67d958f / 78a9bf0b / 2e3bd1fa, docs/mixed_kv_speed_plan.md, docs/mixed_kv_page_transport_targets.md, docs/mixed_kv_speed_round1_tracks.json, and a trial merge of wt/A + wt/B done in a throw-away worktree (removed). Cycle→µs uses 1.80 GHz (P0.3's measured SM clock under the co-tenant; P0.5/P0.6 quoted 1.98).

## 1. Status vs targets (docs/mixed_kv_page_transport_targets.md)

All medians, locked runs. "main" = kernel state of the merged tip (sm90 `mha_sm90.cu` on main is still fe0f3b6a's; wt/A and wt/B are unmerged).

| host / mode | A16 ref | target | main tree | wt/A alone | wt/B alone | gate |
|---|---|---|---|---|---|---|
| H200 sm90 q=1 FP8 (mha_sm90.cu) | 82.0-83.0 (transport_a16) | <= 58 | 91.0-91.8 | 88.7/88.8 | **77.2/77.3** | fail |
| H200 sm90 q=1 FP4 | " | <= 36 | 95.9-96.4 | 92.7/92.7 | **73.1/73.3** | fail |
| H200 sm90 q=1 mixed | " | <= 62 | 114.1-115.7 | 109.4/109.7 | **83.4/83.5** | fail |
| H200 sm90 q=1 A16 (transport) | 108-110 stock mha.cu | parity | 82.0-83.0 | 82.0/82.3 | 81.8/81.8 | pass (kernel-side 14 µs above the 67.5 µs achievable-read floor) |
| H200 q=4 FP8 (mha.cu SPEC_DEC) | 136.4 | <= 94 | **198.7** (after [40]) | — | — | fail |
| H200 q=4 FP4 | " | <= 59 | **236.1** | — | — | fail |
| H200 q=4 mixed | " | <= 101 | **216.4** (0.915x fp4; was 422.8 after [29], 436.9 before; Track S acceptance <= 1.5x met) | — | — | fail |
| RTX 5090 q=1 FP8 | 174.3 | <= 125 | **100.5** (after [29][26][27]+78a9bf0b; was 139.5) | — | — | pass |
| RTX 5090 q=1 FP4 | " | <= 79 | **59.5** (was 83.4) | — | — | pass |
| RTX 5090 q=1 mixed | " | <= 135 | **113.5** (was 145.8) | — | — | pass |
| RTX 5090 q=4 FP8 | 179.1 | <= 128 | 125.0 after [29]; 122.7 after [26][27]+78a9bf0b | — | — | pass |
| RTX 5090 q=4 FP4 | " | <= 81 | 81.9 after [29]; 81.9 after [26]; **84.8** in the 78a9bf0b run (spread 2-4 µs) | — | — | fail by 0.9-3.8 µs |
| RTX 5090 q=4 mixed | " | <= 138 | 132.3 after [29]; 124.5 after [26][27]+78a9bf0b | — | — | pass |
| H200 FA3 A16 q=1 / q=64 | stock 299.2 / 307.9 | <= 315 | **282.0 / 287.0** (0.94x; was 525.3) | — | — | pass |
| H200 FA3 FP8 | " | <= 330 | 736.9 / 747.7 (was 1477.8) | — | — | fail ([23] pending) |
| H200 FA3 FP4 | " | <= 330 | 944.1 / 963.6 | — | — | fail |
| H200 FA3 mixed (dynamic module) | " | <= 330 | 1759.9 / 1761.9 (unchanged by [22]) | — | — | fail |

A+B combined on sm90 q=1 has **not** been measured; prediction in section 4. Correctness: A 36/36 (34 bit-exact + 2 independent-reference at atol 2e-3), B 52/52 on H200 and RTX 5090, E 34/34 both hosts, D 66/66, W 34/34 both hosts.

## 2. Phase 0 decisions (gate → branch selected, deciding number)

| item | deciding number | branch selected |
|---|---|---|
| P0.1 [32] | Probe read at the A16 footprint 4.23 TB/s (67.5 µs for 285 MB), 4.5 TB/s sustained; transport_a16 82.7 µs = 3.45 TB/s | "~4.5+ TB/s" branch: A16 plateau is kernel-side per-tile TMA issue → fold into [15]/[34], sized ~14 µs (not 24). Rooflines A16 67.5 / FP8 38.6 / FP4 23.0 / mixed 43.0. Co-tenant rule: repeats x kernel < 1.5 ms per event pair; SM clock 1.80 GHz under it. |
| P0.2 [39] (in E) | q=4 all modes `mha.cu spec_dec=True`; build has no `-DM_TILESIZE` → nbCtaPerSM already 1, REG 251-255; STACK 48/112 + LDL 114/378 identical on sm120 at REG 166-242 | Step 1 no-op; step 2 [29] executed; step 3 re-route rejected by arithmetic (K3/V3 117,760 B > 115,200 cap; mha_sm90 SPEC_DEC mixed kernel is a 16-instr stub) → [40] instead |
| P0.3 [11] | s3-s2 = 540 (gemm1 loaded) vs 544 (skipped); xBar.consumed released 3.4-4k cyc before need; cadence fp4 2672 cyc = T_g0 2048 + K-wait 622 | max()/gemm0-bound: [11] not built; wall = 3 x (13 x T_tile + 4.4 µs fill) + 13.6 µs tail (fp4); consumer floor 1.09 µs/tile, not 1.5 |
| P0.4 [12] | mio+lg <= 2.7 %, no_inst 5-8 %, selected+not_selected 43-48 %, wait+math 34 % (fp4) / 23 % + short_sb 21 % (fp8); fp4 405 SASS/lane-tile (259 = cutlass e2m1 LUT) | Count levers first ([16], audit); [15] justified by registers (wait/ILP 34-44 %); [14] bounded to the fence+arrive share 0.2-0.3 µs (J1's -0.1..-0.2); code-size cuts rejected |
| P0.5 [34] | 10 → 5 warps/scheduler: 1.00 → 0.84 µs/tile (-16 %, not -50 %); 136-CTA run is 2 CTAs/SM (probe), B=16 n=1 is the 1-CTA point (wall 71.9 vs 57.1 default) | Latency-bound: RT-removal levers only; [9]/[38] closed; [15] must not be argued by spin relief |
| P0.6 [5] | slot13→slot8 = 186 → 181 cyc (-3 %, inside spread 175-201); per-lane MEMBAR.ALL.CTA + FENCE.VIEW.ASYNC survive an elected arrive | [5] closed negative; step 2 not scheduled; backends.md level-3 row was in kilo-cycles |
| P0.7 [24] | fin 3.9 µs/pair = expK 1.80 + expV 1.97-2.03 (97 %); wait 0.6 %, fc 1.7 %; producer mio 0.4 %, lg 0, port 15 % of peak; acq 0.10 µs; compiled split already 72/216 | [23] target 0.5-0.6 µs/pair holds (latency/code-shape, not port) with 6 added verify items; [20] closed as no-op; bench rule --repeats 1 for >= 0.5 ms kernels |
| P0.8 [25] | n=5: fp8 +4.0 %, fp4 +13.3 %, mixed +8.3 %; probe (c) 2x bytes same requests: fp8 139.8 → 125.5 (0.897x); per-CTA fixed cost 4.8-7 µs | Branch (b) request-rate-bound: [26] → [27] → [29] executed; [25] host default rejected; per-CTA fixed cost is a new Track W item |

## 3. Merge order onto `claude/mixed-kv-sm90-tma`

Already merged (verified `git merge-base --is-ancestor`): wt/p07 (6d5ae5a6, FinTrace on main), wt/p08 (78ffe847, MIXED_KV_PROBE_C on main), wt/p01 (0ca029ee: `last_xqa_dispatch`, kernel_family bench lines, probe_hbm_bandwidth.py on main), wt/p06 (99f2c5a0: docs only — the elected arrive is correctly absent), wt/p03 (f722e641: docs + `xqa_mixed_trace_once.py`, `microbench/parse_xqa_trace.py`, `sm90_rt_constants.cu`; the kernel-side per-CTA record buffer `g_mixedKvCtaTrace` is **not** on main, so those tools are half dead), wt/E (402926dc [29], fac7842a [40]), wt/D (724ce904), wt/W (0b40f030 [26][27] + 78a9bf0b). P0.4/P0.5 landed directly (08937a62, 3e811794). Remaining: **wt/B, then wt/A** (either order is kernel-conflict-free by `merge-tree`; B first honours the plan's "B owns the barrier init block").

Trial merge results (exact):

1. **main + wt/B**: `git merge-tree` clean (rc 0). `csrc/xqa/mhaUtils.cuh` auto-merges: B's hunks (@@411/432/444 drop `asm volatile` in `convertE4M3x2ToA16`/`convertE2M1x2ToA16`; @@538 +62 lines `prmtSelfB32/prmtB32/e4m3x4ToBF16x2Pow2m120/e2m1x8ToBF16x2Pow2m126/e4m3x4ScalesToFloat/bf16x2BitsFromFloats`) are disjoint from main's [29]/[40]/[26]/78a9bf0b hunks (`selectByIndex`, `HeadPtr::operator+`, the page-outer `copyMixedPartialHeadsAsync`/`expandMixedPartialHeadsInPlace`, ld/st.shared asm). Semantic check required, not textual: sm120's rewritten expansion still calls the two convert helpers whose `volatile` B removed → rerun the runner on ws-1 and compare sm120 module SASS to 78a9bf0b's reference (q=1 fp8 5144 / fp4 5224 / mixed 6960 instr, LDL/STL 0, no generic LD/ST).
2. **(main+B) + wt/A**: `csrc/xqa/mha_sm90.cu` auto-merges (main's copy equals fe0f3b6a; A hunks are :106-160 macros/named-barrier ids, :281-353 SharedMem, :1297-1716 consumer bodies, :2987-3641 colMax/rescale; B hunks are :121 `mixedLoaderTma`, :292-350 SharedMem barrier list, :1202-1241 barrier init, :1867-2160 loader/converters, :2557-2900 copy issue/expansion). Verified in the merged file: `kLoadReady` 0 refs, `gemm0WarpColMax` present, `gemm0NamedBarId 3`/`gemm1NamedBarId 4` (B uses no named barriers → no id collision). Conflicts:
   - `docs/mixed_kv_page_transport_backends.md`: one block (main's P0.2/[29]/[40] sections lines 520-833 vs A's "Phase 1 — sm90 consumer cheap set" 833-1013), both appended at EOF → keep both, main first. B's section then auto-merges after A's. Note: FA3 [21][22] and Track W [26][27]/78a9bf0b have **no** backends.md record (commit bodies + targets doc only) — add them at merge time; the plan names backends.md as the record.
   - `tests/attention/run_xqa_mixed_page_transport.py` (2 blocks: docstring; case loop — A's generic `for name, fn, *fn_args` vs B's native_fp8 loop + `TAIL_CASES` loop) and `tests/attention/test_xqa_mixed_page_transport.py` (5 blocks at ~315-460: A's `test_xqa_mixed_a16_stream_matches_stock_decode` vs B's `TAIL_CASES` + `test_xqa_mixed_page_transport_tails_and_value_ranges`, appended at the same spot) → union (a marker-stripped union parses); both branches changed `_make_transport` at :32 (B adds `regime`) — keep B's signature, A's call `_make_transport(shape, dtype, device, "a16")` needs `regime` to default. Result: 34 + 2 + 18 = 54 cases.
   - `docs/mixed_kv_speed_plan.md` auto-merges (A's Result at Phase 1 :109; E's at :189/:193).
3. **SharedMem**: B removes kLoadReady/vLoadReady and adds the per-chunk metadata ready barrier (sizeof unchanged 110,592); A appends `gemm0WarpColMax[2][4]` (+256 B, SWAP_AB) → 110,848 B; still < 115,200 → verify 2 CTAs/SM with the on-main residency probe (`mixedKvTraceSmResident`, expect 2) and `cudaOccupancyMaxActiveBlocksPerMultiprocessor`.
4. **Trace macros**: A's `MIXED_KV_TRACE_TILES` (:109, default 8, `nbTraceTiles`) and B's slot 11-13 changes ([13]: landed→ready 175 → 50 cyc) auto-merge. p03's kernel probe (`git diff c2309318 1808d4c7 -- csrc/xqa/mha_sm90.cu`, 171 lines) re-applies onto A+B with **3 conflicts** (macro block :109-150, SharedMem trace fields :407-418, kernel entry :1154-1175) → re-apply by hand as a trace-only commit before Phase 3 ([8]'s artifact is its per-CTA start/end histogram). p06's patch applies cleanly but must **not** be re-applied (negative).
5. **Register budget**: A ([1] +2 regs) and B ([16] hoisted lane constants; one intermediate variant spilled 8 B in the mixed build) both sit at REG 48 STACK 0 separately → post-merge `cuobjdump -res-usage` on all four sm90 q=1 modules must show STACK 0; a spill identifies its role by LDL location.
6. **Post-merge verification (before any Phase 2/3 work)**: compile the 4 q=1 + q=4 modules; 54/54 on nkcut2 and ws-1; fp4 static SASS carries both count sets (gemm0 BAR.SYNC 1 / PHASECHK 3 / ARRIVE 12 / ATOMS 0 / DEPBAR 1; gemm1 1 / 7 / 11 / 1; HGMMA 8+8; no kLoadReady SYNCS; `xqa_sm90_converter_sass.py --paths` fp4 K-expand 188 (fallback 223), fp8 187 (222)); one trace set with **K-converter, V-converter, gemm0 and gemm1 periods on the same launches** (see section 4, the pacing question); 5x5 locked bench vs main.
7. In flight, uncommitted: wt/F23 ([23], 526-line diff in `sparse_mixed_mainloop.cuh` + run_fa3/mixed_page_prefill.py/dataflow doc; base db501194 has no FA3-file delta vs HEAD → trivial rebase); wt/E ("Track S step 4 [41] 2-CTA/SM SPEC_DEC build via M_TILESIZE 16 / 64 B K parts, [42] host `chooseNbSubSeqPerSeq`" in `mha.cu`, `flashinfer/jit/xqa.py`, +128 doc lines). IDs [41]/[42] are therefore taken.

## 4. Next round, predictions updated by Phase 0

**Accounting now in force**: wall = 3 x (13 x T_tile + ~4.4 µs fill) + tail (13.6 µs fp4 at the old cadence), 39 tile-times, not 33; cadence = max(role periods) + one handoff RT (~190 cyc); consumer floor 1.00 µs/tile at 2 CTAs/SM; converter fence+arrive = 186 cyc, not 0.45 µs.

**A+B combined (the first measurement of the round)**. B's trace after [16]: fp4 gemm0 cadence 2544 cyc, K-converter period 2228, V expansion 1068 vs K 750 (same code) → a V period ≈ 2546 would coincide with the cadence. A's chain: converters-skipped cadence 2039 → 1884, production T_g0 2341 → 1792. Two readings: (i) B's ("gemm0 paces"): A's −550 cyc T_g0 drops fp4 to the K period 2228 → −316 cyc = −0.18 µs x 39 = **−6.8 µs → fp4 ≈ 66**; fp8 2702 → 2432 = −5.9 → **≈ 71**; mixed 3254 → 2584 = −14.5 → **≈ 69**. (ii) V-converter paces: only A's issue-relief carries over (A measured −3.7/−2.7/−5.5 at the old converter) → **fp4 70-72, fp8 74-77, mixed 78-83**. The same-launch period trace decides; a steady-state converter period 14 % below the cadence (2228 vs 2544) cannot both be right.

**Phase 2** — [5] step 2 closed (P0.6). [14]: payoff bounded by the measured fence+arrive segment, ≤ 186 cyc = 0.1 µs/tile → ≤ −4 µs, only on a converter-paced mode (fp8/mixed if (i)); do after the pacing role is known. [19]: ≤ 0.05-0.1 µs/tile → ≤ −2..−4 µs, only if the copy-issue segment (686-693 cyc in P0.6; 1242 in B's fp4 = absorbed stage-release wait) stays exposed. **New item (proposed [43]) V-converter parity**: V expansion 1068 vs K 750 cyc fp4 with identical SASS; artifact = level-3 V slots + P0.4-style PC sampling of the V expansion PC range (candidates: smem-port contention with gemm1's HGMMA V operand reads / X STSM, vBar ordering); prediction if V reaches K and V paces: −250..−318 cyc/tile = −5.4..−6.9 µs fp4, −5.9 fp8, up to −14 mixed. Audit residual after [16]: 188 vs the 180 target (floor 139 + 13 scale conversion + 6 vote); the remaining 30 glue ≤ 0.15 µs/tile — stop.

**Phase 3** — [8] persistent 264-CTA pull: fills 3 → 1 = −8.8 µs; tail 13.6 → ~3-5 (P0.3 upper bound caveat: lone CTAs finish faster, ≤ 9 µs of tail is real) → **−12..−19 µs at unchanged T_tile** on fp8/fp4/mixed; from A+B: fp8 71-77 → 52-65, fp4 66-72 → 47-60, mixed 69-83 → 50-71. Accept −12 % or better at unchanged T_tile (trace); calibration: XQA_NB_SUB_SEQ=3 gave 1.06/1.07/1.11 vs pure-tile 1.128, consistent with F ≈ 4.5 µs. Condition from P0.8/P0.3: the next item's metadata + first copies must be prefetched during the current item's last tiles, otherwise the 4.36 µs (fp4) / 7.75 µs (a16) first-K latency is paid per item and the fill saving is 0. [36a][36b]: clk0 → K-loader start 3864 cyc (2.1 µs) precedes first K ready 7828 cyc → −2..−4 µs unchanged. [37] PDL stays outside gate numbers.

**Phase 4** — [15]/[34]: justification is registers (P0.4 wait+math+short_sb 34-44 % of expansion), not spin relief (P0.5). Post-[16] the expansion is 750 cyc fp4 / 816 fp8, so the ILP ceiling is 255-330 cyc = 0.14-0.18 µs/tile → **−5.5..−7 µs** when converter-paced; converter period 2228 → 1900-2000 cyc (accept ≤ 2000 cyc = 1.1 µs, replacing "≤ 2.2 µs"). Its loader merge / per-warp TMA issue is also P0.1's A16 lever: transport_a16 81-83 → toward 67.5 (≤ −14 µs; also lowers mixed's A16 tiles). [7] L8: gate "consumer within 20 % of converter period" is met (1884/2228 = 0.85) but wall effect is 0 until the converter is below ~1900 cyc → order [8] → [15] → [7]; [7] is the FP4-pursuit item (consumer 0.25-0.3 µs/tile).

**Track F** — [23] holds (P0.7) with the amended verify list: shared-window LDS/STS with 32-bit addresses (LD.E/ST.E in producer = 0), one global-scale LDG per operand per item (0 in pair body), format branch outside the block loop (BSSY in expansion = 0), all 128 threads (warp 3 idle = 12 % of producer samples today), LDS.32 scale words + 4-B scale copies (1.11 M + 1.95 M excess wavefronts), producer region ≤ ~600 SASS (no_inst + branch_resolving 11-14 %). Prediction: fin 3.9 → ≤ 0.6 µs/pair; fp8 737-748 / fp4 944-964 → **300-330**; mixed 1760 (dynamic module 5263 SASS, unchanged by [22]) → 300-330 only with a per-page format branch in the dynamic arm ([40]'s pattern) — add to [23]'s scope. [20] closed (split already 72/216; dead constants removed in b54d0717). Timing rule: `--repeats 1` (repeats 5 doubles ≥ 0.5 ms kernels under the co-tenant).

**Track W** — branch (b) done: [26] q=1 fp8 113.8 → 101.5, fp4 65.6 → 57.4, mixed 127.1 → 120.0 (cp.async.cg variant rejected: fp8 q=4 122.5 → 177.1); [27] fp8 → 100.7/101.0, mixed → 117.9/118.8 (FP4 excluded: 57.4 → 71.7 with GRP_LOAD_V); 78a9bf0b mixed → 113.5. Open: FP4 q=4 81.9-84.8 vs ≤ 81 — first a 3-round locked A/B with spread, then [28] (issue-only; FP4 is SM-side bound) or the per-CTA fixed-cost item (4.8-7 µs/CTA must drop below ~1 µs before any grid-fill gain; FP4 q=1 59.5 → ~48 only then). [25] host default rejected (n=5: +4/+13/+8 %); E's uncommitted [42] model keeps n=1 at 170 slots, consistent.

**Track S (sm90 q=4)** — 198.7 / 236.1 / 216.4 vs 94 / 59 / 101 at 1 CTA/SM, 0.4 issued instr/scheduler-cycle, DRAM 7-14 %. E's uncommitted step 4: [42] `chooseNbSubSeqPerSeq` n=4 at 132 slots (measured 0.85x → ≈ 169 / 201 / 184); [41] 2-CTA/SM SPEC_DEC via M_TILESIZE 16 (SharedMem ~87 KB, launch_bounds(256,2), 128-reg cap; cited REG 128 STACK 8-24 on q=1 modules → STACK 0 is the artifact to demand). Even at a 0.55-0.6x occupancy gain the targets need ≥ 2.1x on FP8 — not reachable this round.

## 5. Gate check (50 % of analytic), updated

- **H200 sm90 q=1** (A16 82-83; targets FP8 ≤ 58, FP4 ≤ 36, mixed ≤ 62): measured best FP8 77.2 / FP4 73.1 / mixed 83.4 (wt/B alone). Predicted A+B 71-77 / 66-72 / 69-83; after [8] 52-65 / 47-60 / 50-71; after [15] 45-60 / 40-55 / 43-64. **FP8 borderline pass** (needs A+B ≤ ~71 and [8] ≥ −13 µs); **mixed borderline** (needs the V/A16-tile residual gone: mixed period 2584 vs fp8 2432 after [13]/[16] — the plan's "mixed ≤ fp8 period" check is not yet met, 3254 vs 2702 cadence); **FP4 fails** (predicted floor 40-55 vs 36, as the plan stated; the fp4 lane-tile is 188, not the ≤ 150 that would reopen it). A16 parity holds; 14 µs kernel-side headroom is [15]'s.
- **H200 sm90 q=4** (A16 136; targets 94/59/101): 198.7 / 236.1 / 216.4 — **all fail**; Track S acceptance (mixed ≤ 1.5x fp4) met at 0.915x. Next: [42] (−15 %), [41] (occupancy), both in E's uncommitted tree.
- **RTX 5090 sm120** (A16 174; targets 125/79/135): q=1 **passes all three** (100.5 / 59.5 / 113.5 = 1.73x / 2.93x / 1.53x vs analytic 1.87 / 3.56 / 1.66); q=4 FP8 122.7-125.0 and mixed 124.5-132.3 pass, **FP4 81.9-84.8 vs ≤ 81 open** (within the 2-4 µs run spread; decide with 3 locked rounds).
- **H200 FA3** (stock 299.2/307.9; parity): **A16 passes** (282.0 / 287.0 = 0.94x, ≤ 315); FP8 737-748, FP4 944-964, mixed 1760 **fail pending [23]** (predicted 300-330, ≤ 1.1x; mixed additionally needs the dynamic-module per-page dispatch).
- Measurement rules carried forward: H200 bursts < 1.5 ms per event pair (repeats 5 below ~300 µs, 2-3 for q=4 mixed, `--repeats 1` for FA3), min/median/max reported, SM clock 1.80 GHz for cycle conversions, baselines from a pristine checkout (a cached workspace rebuilds from current source), rsync'd checkouts need re-pointed `flashinfer/data/{csrc,include}` symlinks (B's trap: two "baseline" runs were the new code).